import React from 'react'

const EpisodeList: React.FC = () => {
  return (
      <section>
        <h1 className="mv0 normal">Episode List</h1>
      </section>
  )
}

const styles = {
  header: `
    pa3
    sans-serif
    bg-light-blue
`
}

export default EpisodeList
